{% for canal in canalesItem %}
        <!--  Función para insertar usuario    MODIFICAR USUARIO  -->
        <a onclick="insertarUser(this)" class="dropdown-item" idGrupo="{{canal.idCanal}}" href="#"> {{canal.nombre}} </a>
{% endfor %}
